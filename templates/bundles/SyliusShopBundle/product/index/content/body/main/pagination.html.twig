{% import '@SyliusShop/shared/pagination/pagination.html.twig' as pagination %}
<div class="noho-list-pagination">
    {{ pagination.simple(hookable_metadata.context.resources.data) }}
</div>
</div>{# noho-catalog-product-list (opened in products.html.twig) #}
