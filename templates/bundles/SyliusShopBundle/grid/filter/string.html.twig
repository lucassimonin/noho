{% form_theme form '@SyliusShop/form/theme.html.twig' %}

{% if form.type is defined %}
    {{ form_row(form.type, {'label': filter.label}) }}
{% endif %}
{{ form_widget(form.value, {attr: {placeholder: 'sylius.ui.search'|trans, class: 'form-control-sm'}, 'label': form.type is defined ? form.value.vars.label : filter.label}) }}
